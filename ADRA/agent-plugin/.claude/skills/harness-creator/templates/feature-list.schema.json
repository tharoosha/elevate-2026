{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Harness Feature List",
  "description": "State/scope tracking for agent-driven work, generated by harness-creator.",
  "type": "object",
  "required": ["features"],
  "properties": {
    "features": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "name", "description", "dependencies", "status"],
        "properties": {
          "id": {
            "type": "string",
            "description": "Stable identifier, referenced by other features' dependencies."
          },
          "name": {
            "type": "string"
          },
          "description": {
            "type": "string"
          },
          "dependencies": {
            "type": "array",
            "items": { "type": "string" },
            "description": "IDs of features that must be status=pass before this one may be started."
          },
          "status": {
            "type": "string",
            "enum": ["not-started", "in-progress", "blocked", "pass"]
          },
          "evidence": {
            "type": "string",
            "description": "Populated only after verification has actually been run — real command output, not a self-report. Absent or empty until then."
          }
        }
      }
    }
  }
}
